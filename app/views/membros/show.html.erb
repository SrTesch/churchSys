<p style="color: green"><%= notice %></p>

<%= render @membro %>

<div class="btn-group" role="group">
  <%= link_to "Editar", edit_membro_path(@membro), class:"btn btn-secondary me-2" %>
  <%= button_to "Apagar", @membro, method: :delete, class:"btn btn-secondary me-2" %>
  <%= link_to "Voltar", membros_path, class:"btn btn-secondary"%>
</div>
